{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../wwwroot",
    "types": [
      "jquery",
      "bootstrap",
      "jqueryui"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts",
    "../node_modules",
    "../wwwroot"
  ]
}